import React from 'react';
import AppLayout from '@/components/AppLayout';
import GatePassContent from './components/GatePassContent';

export default function GatePassPage() {
  return (
    <AppLayout>
      <GatePassContent />
    </AppLayout>
  );
}
